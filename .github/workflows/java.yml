name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: eks_runner
    container:
      image: techiescamp/java-agent:2.0.0
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2
      with:
        repository: kubernetes-learning-projects/kube-petclinc-app
        ref: main

    - name: Checkout Repository
      uses: actions/checkout@v2
      with:
        repository: techiescamp/kubernetes-projects
        ref: java-app-deployment
        submodules: 03-java-app-deployment

    - name: Command
      run: |
        ls
        ls 03-java-app-deployment

    - name: Create Namespace if not exists
      run: |
        kubectl create namespace pet-clinic-app || true
        kubectl create namespace pet-clinic-db || true

    - name: Deploy MySQL Database
      run: |
        kubectl apply -f 03-java-app-deployment/secret.yml --namespace=pet-clinic-db
        kubectl apply -f 03-java-app-deployment/mysql/configmap.yml
        kubectl apply -f 03-java-app-deployment/mysql/mysql.yml

    - name: Deploy Java Application
      run: |
        kubectl apply -f 03-java-app-deployment/secret.yml --namespace=pet-clinic-app
        kubectl apply -f 03-java-app-deployment/java-app/configmap.yml
        kubectl apply -f 03-java-app-deployment/java-app/java.yml